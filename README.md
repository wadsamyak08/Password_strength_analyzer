# Password_strength_analyzer
A Python desktop tool that checks password strength in real time using Tkinter and SQLite. It analyzes length, complexity, and entropy, detects weak patterns, suggests improvements, and blocks reuse of old passwords via a secure hash vault — all in a lightweight, modern UI.
